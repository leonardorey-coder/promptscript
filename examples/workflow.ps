log("Comenzando workflow")

modelo = LLMClient({
  provider: "openrouter",
  apiKey: "OPENROUTER_API_KEY",
  model: "mistralai/devstral-2512:free",
  no_ask: true,
  maxTokens: 16384,
})

# Crear landing page completa en un solo paso
log("Creando landing page completa de gatitos...")
plan = modelo("Crea public/index.html - una landing page COMPLETA y PROFESIONAL de gatitos.")
apply(plan)


log("Workflow terminado!")